import React from 'react'
import './Services.css'
const Services = ({ darkMode }) => {
  return (
    <div id='Services' className={`${darkMode ? "dark-mode-services" : ""}`} >
      Services.coming soon....!
    </div>
  )
}

export default Services
